<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brad.scheduling_final_year">
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".meetingsActivity" />
        <![CDATA[
        android:label="@string/app_name">
        ]]>
        <activity
            android:name=".addMeetingPopUp"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Theme" />

        <activity
            android:name=".ViewEventDetailedPopUp"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Theme" />

        <activity android:name=".todaysActivity"></activity>
        <activity android:name=".AppointmentsActivity"></activity>
        <activity android:name=".DeadlinesActivity"></activity>
        <activity android:name=".HobbiesActivity"></activity>
        <activity android:name=".OtherActivity"></activity>
        <activity android:name=".editEventActivity"
            android:theme="@style/AppTheme.Theme">
        </activity>
        <activity android:name=".CalendarEvents"></activity>


    </application>

</manifest>